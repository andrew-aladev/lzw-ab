language: c

dist: focal
osx_image: xcode11.6

jobs:
  include:
    - os: linux
      arch: amd64
    - os: osx
      arch: amd64

addons:
  apt:
    packages:
      - cmake
      - libgmp-dev
  homebrew:
    packages:
      - gmp
    update: true

script:
  - scripts/toolchains.sh
